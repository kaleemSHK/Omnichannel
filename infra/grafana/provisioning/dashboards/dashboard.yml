apiVersion: 1
providers:
  - name: BlinkOne
    folder: BlinkOne
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards
