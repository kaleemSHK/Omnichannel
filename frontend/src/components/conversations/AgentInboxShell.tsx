'use client';

/**
 * @deprecated Use ConversationsPage layout (ConversationList + MessageThread + AgentAssistPanel).
 * Kept for reference; calling inbox lives under /calling.
 */
export function AgentInboxShell() {
  return null;
}
