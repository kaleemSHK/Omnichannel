# BlinkOne sidecar base — Node 22 Alpine, non-root
FROM node:22-alpine AS base
RUN corepack enable && corepack prepare pnpm@9.15.4 --activate
WORKDIR /app
RUN addgroup -g 1001 blinkone && adduser -u 1001 -G blinkone -D blinkone
USER blinkone
